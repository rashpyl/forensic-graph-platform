namespace ForensicGraph.Application;

/// <summary>
/// Anchor type used for assembly scanning (validators, MediatR handlers, etc.).
/// Do not add members — its only purpose is to identify this assembly.
/// </summary>
public static class AssemblyMarker
{
}
