# Copy to .env for local development (already gitignored).
# These values are consumed by docker-compose.yml.

POSTGRES_USER=forensic
POSTGRES_PASSWORD=forensic
POSTGRES_DB=forensic_graph
POSTGRES_PORT=5432
